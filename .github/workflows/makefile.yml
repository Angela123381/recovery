name: Makefile CI

on:
  push:
    branches: [ Android-9.0-old ]
  pull_request:
    branches: [ Android-9.0-old ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    
    - name: configure
      run: ./configure
      
    - name: Install dependencies
      run: make
      
    - name: Run check
      run: make check
      
    - name: Run distcheck
      run: make distcheck
